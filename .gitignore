# Dependencies
node_modules/
vendor/

# Builds
dist/
web/dist/
*.local

# Environments & secrets — never commit
.env
.env.*
!.env.example
!.env.deploy.example
.env.deploy
*.key
api-key*.txt
secrets/

# Golden Prompt — private by design (cahier §7)
assets-existants/golden-prompt/

# Twin_v9 — les GABARITS sont confidentiels (ADR-010) : jamais dans le dépôt.
# Source locale : ../Twin_v9 (hors repo). Oracles de parité générés à la
# demande (scripts/twin9/gen-oracles.sh) dans engine/test/twin9-oracles/.
golden-twin9/
engine/test/twin9-oracles/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Test artifacts
coverage/
playwright-report/
test-results/

# PHP
composer.phar
.phpunit.result.cache
web/public/data/
build/
backups/
