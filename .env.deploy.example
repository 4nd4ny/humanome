# Copy to .env.deploy (gitignored) and fill in — see docs/hebergement.md
FTP_HOST=ftp.clusterNNN.hosting.ovh.net
FTP_USER=
FTP_PASSWORD=
# Explicit FTPS (AUTH TLS). Set to false only if the server refuses TLS.
FTP_SECURE=true
